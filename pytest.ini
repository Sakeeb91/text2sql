[pytest]
addopts = --cov=app --cov-report=term-missing --cov-fail-under=85 --strict-markers
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning:fastapi.*
    ignore::DeprecationWarning:pydantic.*
    ignore:unclosed database in:ResourceWarning
