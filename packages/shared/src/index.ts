/**
 * Main entry point for the @text2sql/shared package.
 *
 * This module exports all shared types and utilities that can be
 * imported by other packages in the monorepo.
 *
 * @example
 * ```typescript
 * import { QueryRequest, QueryResponse } from '@text2sql/shared';
 * ```
 */

export * from './types';
